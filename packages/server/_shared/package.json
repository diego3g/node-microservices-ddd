{
  "name": "@server/shared",
  "description": "Shared between all server packages.",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "babel src --root-mode upward --extensions \".js,.ts\" --ignore *.spec.ts --out-dir dist --copy-files",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.17.1",
    "shallow-equal-object": "^1.1.1",
    "uuidv4": "^6.1.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.1",
    "typescript": "^3.9.3"
  }
}
